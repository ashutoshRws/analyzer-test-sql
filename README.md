# analyzer-test-sql

Minimal SQL schema/migration fixture for analyzer language-detection testing.

## Expected analyzer behavior

- **Primary language**: SQL
- **Secondary/config**: YAML
- **Min source files detected**: 3
- **Key paths**: `src/`, `tests/`, `schema/manifest.sql`

## Run locally

Apply scripts with your SQL client (PostgreSQL, SQLite, etc.).
